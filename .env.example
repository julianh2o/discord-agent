# Discord Bot Token (required)
# Get this from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Ollama Configuration (used by BAML agent)
# These are read by BAML from environment variables
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Whisper API Configuration (for voice message transcription)
# Point this to your Whisper server (e.g., faster-whisper-server, whisper-asr-webservice)
# The bot expects an /asr endpoint that accepts multipart form data with 'audio_file'
WHISPER_URL=http://localhost:9000

# Comma-separated list of channel IDs where the bot will respond
# Leave empty to respond in all channels (not recommended for production)
# Example: ALLOWED_CHANNEL_IDS=123456789012345678,987654321098765432
ALLOWED_CHANNEL_IDS=
